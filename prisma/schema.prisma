generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  id    Int     @id
  name  String  @db.VarChar(50)
  email String? @db.VarChar(20)
}
